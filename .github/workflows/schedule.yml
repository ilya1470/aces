name: Run ACES Scraper

on:
  push:                # <--- Add this block
    branches:
      - main
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  scrape:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run scraper
        env:
          # Ilya
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}

          # Staging
          SUPABASE_URL_STAGING: ${{ secrets.SUPABASE_URL_STAGING }}
          SUPABASE_SERVICE_ROLE_KEY_STAGING: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY_STAGING }}

          # Main
          SUPABASE_URL_MAIN: ${{ secrets.SUPABASE_URL_MAIN }}
          SUPABASE_SERVICE_ROLE_KEY_MAIN: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY_MAIN }}

          # ACES credentials
          ACES_USERNAME: ${{ secrets.ACES_USERNAME }}
          ACES_PASSWORD: ${{ secrets.ACES_PASSWORD }}
        run: python scraper.py
